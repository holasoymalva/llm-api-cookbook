# Groq

This folder focuses on creating high-performance, low-latency applications using **Groq**'s LPU™ Inference Engine.

## Setup

1.  Get an API Key from [Groq Console](https://console.groq.com/).
2.  Set `GROQ_API_KEY` in your `.env` file.
3.  Examples use the official `groq-sdk` for TypeScript/Python where available, or OpenAI-compatible client configuration if needed.
