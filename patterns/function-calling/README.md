# Function Calling (Tools)

Examples of defining tools/functions and letting the LLM decide when to call them.

## Examples

-   **Defining functions**: Schema definition.
-   **Handling calls**: parsing arguments and executing the function.
-   **Returning results**: passing the function output back to the LLM.
