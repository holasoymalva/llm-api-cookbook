# Structured Output / JSON Mode

Examples of forcing the LLM to output valid JSON or a specific schema.

## Examples

-   **JSON Mode**: Using `response_format: { type: "json_object" }`.
-   **Structured Decoding**: Using libraries or provider features to ensure schema compliance (e.g., Pydantic with OpenAI/Instructor).
